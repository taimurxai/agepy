import threading
from datetime import datetime, timezone

FRAUD_FILTER_URL = ''

def upload_file_to_fraud_api(file_type, filename, raw_bytes, token, proxies=None):
    return True

def send_fraud_report(verification_token, dob, altered_id_hash, selfie_hash, video_hash, timecodes, user_agent=None):
    return None
